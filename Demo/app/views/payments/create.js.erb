// var stripe = Stripe("<%= Rails.application.secrets.stripe_publishable_key %>");

// stripe.redirectToCharges({
// 	sessionId: '<%= @session.id %>'
// }).then(function (result){
// 	console.log(result.error.message);
// });
